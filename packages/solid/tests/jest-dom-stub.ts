// Stub file: prevents vite-plugin-solid from auto-injecting
// @testing-library/jest-dom/vitest as a setup file.
// The plugin skips injection when setupFiles already contains a path
// matching /jest-dom/. These tests run in Node and do not need DOM matchers.
