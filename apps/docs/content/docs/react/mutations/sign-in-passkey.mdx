---
title: useSignInPasskey
description: Passkey (WebAuthn) sign-in mutation.
---

Resets the session query on success so the new session is refetched.

## Usage

```tsx
import { useSignInPasskey } from "@better-auth-ui/react"

const { mutate: signInPasskey } = useSignInPasskey()

signInPasskey({ autoFill: false })
```

## Options factory

```tsx
import { signInPasskeyOptions } from "@better-auth-ui/react"
import { useMutation } from "@tanstack/react-query"

const { mutate } = useMutation(signInPasskeyOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/react/src/hooks/auth/use-sign-in-passkey.ts" name="UseSignInPasskeyParams" />
