---
title: Sign In (Email)
description: Email/password sign-in mutation.
---

Resets the session query on success so the new session is refetched.

## Usage

```tsx
import { useSignInEmail } from "@better-auth-ui/react"

const { mutate: signInEmail, isPending } = useSignInEmail()

signInEmail({
  email: "alice@example.com",
  password: "hunter2",
  rememberMe: true
})
```

## Options factory

```tsx
import { signInEmailOptions } from "@better-auth-ui/react"
import { useMutation } from "@tanstack/react-query"

const { mutate } = useMutation(signInEmailOptions(authClient))
```

## Props

<auto-type-table path="../../../../../../packages/react/src/hooks/auth/use-sign-in-email.ts" name="UseSignInEmailOptions" />
